{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug farert (LLDB)",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/farert",
      "args": ["高麗川", "八高線", "高崎", "上越新幹線", "大宮"],
      "env" : {
	"farertDB" : "${workspaceFolder}/../../../db/jrdbNewest.db"
      },
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "build (make DEBUG=1 farert)"
    }
  ]
}
