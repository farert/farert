git

# machine A

$ mkdir ~/any_proj_dir
$ cd ~/any_proj_dir
$ git init --separate-git-dir=$HOME/Dropbox/any_proj_repo   # Dropbox‰º‚Éì¬‚³‚ê‚é
$ echo "<?php phpinfo();" > miracle.php
$ git add .
$ git commit -m "First repository regist."

.. on another machine(machine B) ..

$ mkdir ~/proj_on_another_machine_dir
$ cd proj_on_another_machine_dir
$ echo "gitdir: $HOME/Dropbox/any_proj_repo" > .git	# same as ~/any_proj_dir/.git at machine A'.
$ git config core.worktree `pwd`
$ git checkout HEAD .
$ echo "var_dump(trim('0') == false); // true" >> miracle.php
$ git add -u
$ git ci -m "’ˆÓ‘‚«‚ğ’Ç‰Á"


