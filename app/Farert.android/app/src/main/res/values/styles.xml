<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.FlatButton" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">#81d4fa</item>
        <item name="android:textColor">#01579b</item>
    </style>
    <style name="faretype_spin">
        <item name="android:textColor">#44ceff</item>
        <item name="android:textSize">12sp</item>
    </style>
    <style name="FolderDrawCheckBox" parent="Theme.AppCompat.Light">
        <!-- 枠線 -->
        <item name="colorControlNormal">#ffffff</item>
        <!-- チェック時の色 -->
        <item name="colorControlActivated">#77ff77</item>
    </style>
    <style name="TicketListTitle" parent="Theme.AppCompat.Light">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">#808080</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>
