<resources>

    <!-- Base application theme - Material3 -->
    <style name="AppTheme" parent="Theme.Material3.DayNight">
        <!-- Material3 color scheme -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="colorPrimaryContainer">@color/colorPrimaryDark</item>
        <item name="colorOnPrimaryContainer">@android:color/white</item>
        <item name="colorSecondary">@color/colorAccent</item>
        <item name="colorOnSecondary">@android:color/white</item>
        <!-- Status bar color -->
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Legacy styles updated for Material3 compatibility -->
    <style name="FolderDrawCheckBox" parent="Theme.Material3.DayNight">
        <!-- Material3 checkbox colors -->
        <item name="colorControlNormal">@color/colorChkBoxFolder</item>
        <item name="colorControlActivated">#77ff77</item>
    </style>
    
    <style name="TicketListTitle" parent="Theme.Material3.DayNight">
        <item name="android:textColor">#CFD8DC</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">16sp</item>
    </style>
    
    <style name="TicketListFare" parent="Theme.Material3.DayNight">
        <item name="android:textColor">#ffffff</item>
        <item name="android:textStyle">normal</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="TicketListKm" parent="Theme.Material3.DayNight">
        <item name="android:textColor">#ffffff</item>
        <item name="android:textStyle">normal</item>
        <item name="android:textSize">12sp</item>
    </style>
</resources>
